# epoll basics

Goal: create and close an epoll instance without sockets to verify toolchain and headers on Linux. Print a clear message on non Linux platforms.

Build: added by parent CMake. Run target `epoll_basics`.
